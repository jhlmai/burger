<div class="header-container">
  <img id="hamburger-image" src="assets/img/burger.jpg">
  <header>Eat Da Burger!</header>
</div>

<h1>Burgers!</h1>

<h2>Burgers that are ready to be DEVOURED!</h2>

<ul>
  {{#each burgers}}
    {{#unless devoured}}
      {{> burgers/cat-block devoured=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Burgers that have been DESTROYED!</h2>

<ul>
  {{#each burgers}}
    {{#if devoured}}
      {{> burgers/cat-block devoured=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="ca" name="name">
  </div>

  <div class="form-group">
    <label for="devoured">devoured?</label><br>
    <input type="radio" name="devoured" value="1" checked> devoured!<br>
    <input type="radio" name="devoured" value="0"> Awake
  </div>

  <button type="submit">Add a Burger</button>
</form>